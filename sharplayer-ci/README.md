# SharpLayer Android CI

This branch contains the SharpLayer Android project archive and a GitHub Actions workflow that restores it, builds a debug APK with Android SDK 36 and Gradle 8.11.1, and uploads the APK as the `SharpLayer-debug-apk` artifact.
